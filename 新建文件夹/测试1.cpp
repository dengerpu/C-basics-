#include<stdio.h>
int main()
 {
   int vote,lvote,zvote,wvote,invalidvote;
    lvote=0,zvote=0,wvote=0,invalidvote=0;
	printf("%d\n",vote);
    scanf("%d",&vote);
        while(vote!=-1)
        {
            /*switch (vote)
             {
                case 1: lvote++;break;
                case 2: zvote++;break;
                case 3: wvote++;break;
                case 0: invalidvote++;
                break;
                case 4: invalidvote++;
                break;  */   
			
        }
         scanf("%d",&vote);
        }
       // printf("Li%2d,zhang%2d,wang%2d,invalid%2d\n",lvote,zvote,wvote,invalidvote);

return 0;
}